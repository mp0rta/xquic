# PR5 (L5b validation hardening) — findings

> **Verified against:** `mp0rta/xquic` commits f79899f / 6254402 / 261b143 (PR5 tip on `feat/mp21/L5b-validation-hardening`). Re-audit needed if `xqc_process_path_challenge_frame`, the loss-detection PATH_CHALLENGE retx site (`xqc_send_ctl_detect_lost`), or any CID-picker scoping changes.

Per slim memo pattern: only non-trivial findings. G-P2 / G-P3 fixes live in commit bodies (no row here). Pre-5 verifications retained as forensic anchors.

Spec: <https://datatracker.ietf.org/doc/draft-ietf-quic-multipath/21/>

| ID | spec § | file:line | finding | verdict |
|---|---|---|---|---|
| G-P5 | §3.1.2 ¶2 | `src/transport/xqc_cid.c:453` (`xqc_get_unused_cid` accepts `path_id`); `src/transport/xqc_multipath.c:271,288` + `src/transport/xqc_packet_out.c:1804,1819` (MP CID lookups thread `path_id` explicitly); `src/transport/xqc_packet_out.c:1453` (legacy single-path hardcodes `XQC_INITIAL_PATH_ID = 0`, correct for non-MP); `src/transport/xqc_engine.c:1240-1284` (NAT rebinding reuses existing `path->path_dcid`, no new CID picked); `src/transport/xqc_multipath.c:804-822` (`xqc_conn_find_path_by_scid` returns the path matching `inner_cid->cid.path_id`). xquic does NOT implement legacy RFC 9000 §9.3 active migration with a new DCID; the only address-change handler is NAT rebinding which keeps the existing CID. | Case A: clean by construction. Note the legacy `xqc_write_retire_conn_id_frame_to_packet` at `xqc_packet_out.c:1453` operates only on `conn_initial_path` (single-path RETIRE_CONNECTION_ID); it is distinct from the per-path MP_RETIRE_CONNECTION_ID handler at `xqc_frame.c:~2189`, and the two must not be conflated on re-audit. | clean |
| G-P3 alternate-path | §3.4 ¶3 | `src/transport/xqc_multipath.c:xqc_path_request_abandon` | PR5 emits PATH_ABANDON on the failing path. draft-21 §3.4 ¶3 RECOMMENDS sending PATH_ABANDON for a broken path on another open path; PR7 G-P14 will implement that selection systematically. The §3.1 ¶10 MUST "explicitly close the path" is satisfied locally by the `XQC_PATH_STATE_CLOSING` transition regardless of whether the wire frame reaches the peer. | deferred-to-PR7 |
| Pre-5 cadence | §3.1 ¶10 | `src/transport/xqc_send_ctl.c:xqc_send_ctl_detect_lost` + `src/transport/xqc_multipath.c:xqc_path_validation_on_retx` | PATH_CHALLENGE retx is NOT driven by `XQC_TIMER_PATH_IDLE` (that timer's expiry closes the path; see `src/transport/xqc_timer.c:134-158`). Retransmission flows through the standard loss-detection cycle (`xqc_send_ctl_detect_lost` → `XQC_NEED_REPAIR` → `xqc_send_queue_copy_to_lost`), so the cadence is PTO/RTT-scaled (`loss_delay = 9/8 * max(latest_rtt, smoothed_rtt)`, floor `XQC_kGranularity * 1000` µs). Threshold `XQC_PATH_VALIDATION_MAX_ATTEMPTS = 3` therefore yields a typical timeout window of roughly 3 × loss_delay rather than a fixed wall-clock budget. | recorded |
| Pre-5 buf_size | impl | `src/transport/xqc_packet_in.h:24` + `src/transport/xqc_conn.c:5022-5067` | `packet_in->buf_size` is set to `end - pos` per QUIC packet inside the UDP datagram (see `xqc_conn_process_packet`). For the typical non-coalesced PATH_CHALLENGE datagram (RFC 9000 §8.2.1 mandates 1200B-padded datagrams carrying PATH_CHALLENGE/PATH_RESPONSE), `buf_size` equals the UDP datagram size. For coalesced packets where PATH_CHALLENGE arrives in a non-first QUIC packet, `buf_size` under-counts the datagram by the preceding packet's bytes. The G-P2 check on this value is conservative: it can falsely reject a malformed-but-just-large-enough coalesced datagram. Acceptable failure mode (the path is closed, peer must reset validation); spec compliance is not weakened. | recorded |
